import { Routes } from '@angular/router';
import { RegisterComponent } from './pages/delivery-person/register/register.component';

export const routes: Routes = [
    {
        path: 'register',  component: RegisterComponent
    }
];
